class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        a=''
        for i in range(len(digits)):
            a+=str(digits[i])
        a=int(a)
        a+=1
        ans=list(map(int,str(a)))
        return ans
